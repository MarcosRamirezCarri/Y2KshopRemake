'use client'
import { useState } from "react"


const LoginModal = ({ShowModal}) =>{
    const [modal, setModal] = useState<boolean>(false)
return(
    <div>
        <div>
            <button>
                Login
            </button>
        </div>

    </div>
)
}